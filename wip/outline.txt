autodrop after 20s
repeat
	left right down
no repeat
	space up z c

while game not over
	if 20s since
		- check if 20s passed since current live
		- harddrop
	else
		- check for input
		- downtick
			- cant stall for 5 ticks when near floor with left right
				- once 5 tick stall, lock
			- reset input is rotation
	- draw blocks
	- update panels
	- check game over


- tick down always
	- should be able to prevent using lr 5 times max
	- 5 max lockin is reset on hold,rotate
- 20s autolock

